{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://kaf.algovoi.co.uk/schemas/receipt.schema.json",
  "title": "KAF sealed run receipt envelope (kaf-receipt-v2)",
  "type": "object",
  "required": ["receipt", "seal"],
  "additionalProperties": false,
  "properties": {
    "receipt": {
      "type": "object",
      "required": ["schema", "seq", "framework", "run", "corpus", "cells", "totals", "sealer", "artifacts", "prev"],
      "additionalProperties": false,
      "properties": {
        "schema": { "const": "kaf-receipt-v2" },
        "seq": { "type": "integer", "minimum": 1 },
        "framework": {
          "type": "object",
          "required": ["name", "version", "design"],
          "properties": {
            "name": { "const": "KAF" },
            "version": { "type": "string" },
            "design": { "type": "string" }
          }
        },
        "run": {
          "type": "object",
          "required": ["id", "purpose", "host", "started", "finished"]
        },
        "corpus": {
          "type": "object",
          "required": ["repo", "commit", "branch"]
        },
        "cells": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["id", "image", "image_digest", "network", "suites", "overall"],
            "properties": {
              "id": { "type": "string" },
              "image": { "type": "string" },
              "image_digest": { "type": "string" },
              "network": { "const": "none" },
              "canary": { "type": ["string", "null"] },
              "suites": { "type": "object", "additionalProperties": { "type": "integer" } },
              "overall": { "type": "integer" },
              "provision_failed_specs": { "type": "array", "items": { "type": "string" } },
              "degraded": { "type": "array", "items": { "type": "string" } }
            }
          }
        },
        "totals": {
          "type": "object",
          "required": ["cells", "suite_executions", "all_green"],
          "properties": {
            "cells": { "type": "integer" },
            "suite_executions": { "type": "integer" },
            "all_green": { "const": true }
          }
        },
        "sealer": {
          "type": "object",
          "required": ["alg", "keyid", "canon_engine", "canon_cross_checks", "canon_cross_check_match", "signer_pkg", "verifier_target"],
          "properties": {
            "alg": { "const": "ed25519" },
            "keyid": { "type": "string" },
            "canon_engine": { "const": "algovoi-substrate" },
            "canon_engine_version": { "type": "string" },
            "canon_cross_checks": { "type": "array", "items": { "type": "string" } },
            "canon_cross_check_match": { "const": true },
            "signer_pkg": { "type": "string" },
            "verifier_target": { "type": "string" }
          }
        },
        "artifacts": {
          "type": "object",
          "required": ["results_tree_sha256"],
          "properties": { "results_tree_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" } }
        },
        "prev": {
          "type": "object",
          "required": ["kind", "sha256"],
          "properties": {
            "kind": { "enum": ["receipt", "p0-manifest"] },
            "name": { "type": "string" },
            "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
          }
        }
      }
    },
    "seal": {
      "type": "object",
      "required": ["keyid", "public_key_hex", "created", "method", "authority", "path", "content_digest", "signature_input", "signature"],
      "additionalProperties": false,
      "properties": {
        "keyid": { "type": "string" },
        "public_key_hex": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "created": { "type": "integer" },
        "method": { "const": "POST" },
        "authority": { "const": "kaf.algovoi.co.uk" },
        "path": { "const": "/kaf/receipt" },
        "content_digest": { "type": "string" },
        "signature_input": { "type": "string" },
        "signature": { "type": "string" }
      }
    }
  }
}
